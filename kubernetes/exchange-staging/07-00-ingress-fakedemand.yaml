apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fakedemand-ingress
  namespace: exchange-staging
spec:
  rules:
  - host: staging.exchange.clickyab.ae
    http:
      paths:
      - path: /exam
        backend:
          serviceName: octopus-fakedemand
          servicePort: 9000
