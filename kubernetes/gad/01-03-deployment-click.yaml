apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: gad
    proc: worker
    spec: click
  name: gad-click
  namespace: gad
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gad
        proc: worker
        spec: click
    spec:
      ImagePullSecrets:
        - name: regsecret
      containers:
      - command:
        - /app/bin/clickworker
        envFrom:
        - configMapRef:
            name: gad-config
        image: registry.clickyab.ae/clickyab/gad:master.343
        name: gad-master
        resources:
          limits:
            memory: 200Mi
          requests:
            memory: 100Mi
      terminationGracePeriodSeconds: 30
